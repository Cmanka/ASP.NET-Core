@model IdentityRole
@{
    ViewBag.Title = "Confirm remove";
}

<div>
    <dl class="dl-horizontal">
        <dt>Name</dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <div>
        <form asp-controller="Role" asp-action="Remove" method="post">
            <div class="form-group">
                <input type="submit" class="btn btn-dark" value="Remove" />
            </div>
        </form>
    </div>
</div>